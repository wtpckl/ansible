- hosts: localhost
  connection: local
  become: true

  pre_tasks:
    - name: Update packages
      community.general.pacman:
        update_cache: yes
        upgrade: yes

  tasks:
    - include: tasks/packages/utils.yml
    - include: tasks/sudo/ansible.yml
    - include: tasks/sudo/wheel.yml
    - include: tasks/systemd/ansible.yml
    - include: tasks/user/ansible.yml
